---
- name: Set service name variable
  ansible.builtin.set_fact:
    service_name: speedtest-tracker
- name: Create folder for Speedtest-tracker config files
  ansible.builtin.file:
    path: "{{ podman_appdata_dir }}/speedtest-tracker/config"
    state: directory
    owner: "{{ podman_user }}"
    group: "{{ podman_group }}"
    mode: "0755"
- name: Copy Speedtest-tracker environment files
  ansible.builtin.copy:
    src: "speedtest-tracker.env"
    dest: "{{ quadlet_dir }}"
    owner: "{{ podman_user }}"
    group: "{{ podman_group }}"
    mode: "0600"
- name: Create Speedtest-tracker Quadlet
  containers.podman.podman_container:
    name: speedtest-tracker
    image: lscr.io/linuxserver/speedtest-tracker:1.13.3@sha256:ce42fcb3c4fdcceb22e8c91ad93b8293ea0fe0873221f07fce36aed65b6a1f51
    state: quadlet
    quadlet_filename: speedtest-tracker
    quadlet_file_mode: "0644"
    quadlet_dir: "{{ quadlet_dir }}"
    ports:
      - "8080:80"
    volumes:
      - "{{ podman_appdata_dir }}/speedtest-tracker/config:/config:Z"
    env_file:
      - speedtest-tracker.env
    quadlet_options:
      - "NoNewPrivileges=true"
      - |
        [Service]
        Restart=always
      - |
        [Install]
        WantedBy=default.target
- name: Open firewalld port for Speedtest-tracker
  ansible.posix.firewalld:
    port: "8080/tcp"
    permanent: true
    immediate: true
    state: enabled
  notify:
    - Reload firewalld
