---
- name: Install Tailscale
  hosts: pbs,pbs_novi,services_srv,monitoring_srv,infisical_srv,pulse
  roles:
    - role: artis3n.tailscale.machine
      vars:
        tailscale_authkey: "{{ lookup('ansible.builtin.env', 'TAILSCALE_KEY') }}"
