---
- name: Create folder for Uptime Kuma data files
  ansible.builtin.file:
    path: "{{ podman_appdata_dir }}/uptime-kuma/data"
    state: directory
    owner: "{{ podman_user }}"
    group: "{{ podman_group }}"
    mode: "0755"
- name: Create uptime-kuma quadlet
  containers.podman.podman_container:
    name: uptime-kuma
    image: docker.io/louislam/uptime-kuma:2.3.2@sha256:9aeb4e51d038047f414309c77a1af553281ca535723cb88907d907269d0a908e
    state: quadlet
    quadlet_filename: uptime-kuma
    quadlet_file_mode: "0644"
    quadlet_dir: "{{ quadlet_dir }}"
    ports:
      - "3001:3001"
    volumes:
      - "{{ podman_appdata_dir }}/uptime-kuma/data:/app/data:Z"
    cap_add:
      - NET_RAW
    quadlet_options:
      - "NoNewPrivileges=true"
      - |
        [Service]
        Restart=always
      - |
        [Install]
        WantedBy=default.target
- name: Open firewalld port for Uptime Kuma
  ansible.posix.firewalld:
    port: "3001/tcp"
    permanent: true
    immediate: true
    state: enabled
  notify:
    - Reload firewalld
